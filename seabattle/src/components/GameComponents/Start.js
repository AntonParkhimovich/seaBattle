import base from "../BaseInit";
class Start{
    constructor(base) {
        this.base = base
    }
    init(){
    base.scene.clear()

    }
    createButton(){

    }
}
const start = new Start(base)
export default  start